
@model GESTION_COLEGIAL.UI.Models.TituloViewModel
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@* Listado *@
<div class="card">
    <div class="card-header" style="background-color:white;">
        <h3>Listado de </h3>
    </div>
    <div class="card-body">
        <table class="table table-striped table-bordered dt-responsive nowrap" id="dataTable">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Descripción</th>
                    <th>Acciones</th>
                </tr>
            </thead>
        </table>
    </div>
</div>


@using (Html.BeginForm("Save", "Titulos", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <div class="container">

        <div class="card">
            <div class="card-header">
                <h3>Registro de Titulos</h3>
            </div>

            <div class="card-body">
                @Html.HiddenFor(model => model.Tit_Id)
                <div class="form-row">
                    @* Columna Descripción *@
                    <div class="col-md-6">
                        <div class="form-group">
                            @Html.LabelFor(model => model.Tit_Descripcion, "Tit_Descripcion", htmlAttributes: new { @class = "control-label" })
                            @Html.EditorFor(model => model.Tit_Descripcion, new { htmlAttributes = new { @class = "form-control" } })
                            @Html.ValidationMessageFor(model => model.Tit_Descripcion, "", new { @class = "text-danger" })
                        </div>
                    </div>
                </div>
                <button type="submit" class="btn btn-primary"><i class="mdi mdi-content-save"></i> Guardar</button>
            </div>
        </div>
    </div>
}

@section Scripts{
    <script src="~/Content/js/pages/titulos.js"></script>
    <script>
        Titulos.datatable({listUrl: "@Url.Action("List")" });
    </script>
    <script>
        Catalogs.configure({
            displayName: "titulos",
            getUrl: "@Url.Action("Find")",
            getUrlDelete: "@Url.Action("Delete")"
        });
    </script>
}